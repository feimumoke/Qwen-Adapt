镜像：
ubuntu20.04-cuda11.8.0-py38-torch2.0.1-tf2.13.0-1.9.5
或者
ubuntu22.04-cuda11.8.0-py310-torch2.1.0-tf2.14.0-1.10.0
pip install --upgrade pip

pip install transformers==4.34.0 accelerate tiktoken einops scipy transformers_stream_generator==0.0.4 peft deepspeed
pip install auto-gptq optimum


------可忽略--------
pip install packaging
pip install flash-attn --no-build-isolation
git clone https://github.com/Dao-AILab/flash-attention
cd flash-attention && pip install .
# Below are optional. Installing them might be slow.
pip install csrc/layer_norm
pip install csrc/rotary

python setup.py install

git config --global http.postBuffer 524288000
------可忽略--------


git clone https://github.com/feimumoke/Qwen-Adapt.git
cd Qwen-Adapt
git checkout feature/fineture
pip install -r requirements_web_demo.txt

Step 1：下载基座模型：
--运行下面代码就会将模型下载到（/mnt/workspace/.cache/modelscope/qwen/Qwen-14B-Chat-Int4）目录
python cli_demo.py

Step2：微调模型:
--使用步骤1中下载的基座模型和数据集进行微调:
bash finetune/finetune_qlora_single_gpu.sh -m /mnt/workspace/.cache/modelscope/qwen/Qwen-14B-Chat-Int4 -d /mnt/workspace/Qwen-Adapt/train_data.json


Step3-使用微调模型:

python web_demo.py

如果报ValueError: Target module QuantLinear() is not supported. Currently, only the following modules are supported: `torch.nn.Linear`, `torch.nn.Embedding`, `torch.nn.Conv2d`, `transformers.pytorch_utils.Conv1D`.
安装：
pip install peft==0.5.0

其他页面显示问题可尝试 package_example_py3.10.txt 中的gradio版本